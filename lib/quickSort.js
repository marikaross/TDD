let foo = [5, 4, 3, 2, 1];

quickSort(array => {
  //let pivot = array.pop();
  //let lessThanPivot = [];
  //let greaterThanPivot = [];
  //loop through array
  if array[i] < pivot, lessThanPivot.push
   //base case: when there's one or fewer elements in array
return array
}

quickSort(foo)