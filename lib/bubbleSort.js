const numbers = [5, 4, 3, 2, 1]

//should loop through from start to finish
//run another for loop concurrently starting at [1] to compare values with other for loop
// if value of second for loop index is greater than 1st for loop, destructure to switch indices.
bubbleSort(array => {
  
}